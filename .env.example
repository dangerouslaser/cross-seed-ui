# CrossSeed UI Environment Variables

# Required: URL to cross-seed daemon API
CROSSSEED_URL=http://localhost:2468

# Required: Path to cross-seed config.js
CROSSSEED_CONFIG_PATH=/config/config.js

# Optional: Web UI port (default: 3000)
PORT=3000

# Optional: Disable authentication (default: false)
# Only enable for trusted network deployments
DISABLE_AUTH=false

# Optional: Logging level (default: info)
# Options: debug, info, warn, error
LOG_LEVEL=info

# Optional: UI database location (default: /app/data/ui.db)
DATABASE_PATH=./data/ui.db

# Optional: JWT secret for session tokens
# If not set, a random secret will be generated on startup
# JWT_SECRET=your-secret-key-here

# Optional: Timezone
TZ=UTC
